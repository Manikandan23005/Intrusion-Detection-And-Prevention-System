[tool.poetry]
name = "client"
version = "0.1.0"
description = "IDS Client"
authors = ["IDS Team"]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
requests = "^2.31.0"
PySide6 = "^6.6.0"
watchdog = "^3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
